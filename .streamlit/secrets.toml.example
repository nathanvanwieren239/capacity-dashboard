# Copy this file to `secrets.toml` in the same folder and set a real password.
# `secrets.toml` is gitignored and must never be committed.
#
# On Streamlit Community Cloud, do NOT upload a file - paste the same line
# into: app menu -> Settings -> Secrets.

APP_PASSWORD = "change-me"
